{{ ansible_managed | comment }}
function iterm2_print_user_vars() {
  iterm2_set_user_var gitBranch $((git branch 2> /dev/null) | grep \* | cut -c3-)
  iterm2_set_user_var awsProfile $(printenv | grep -i aws_profile)
}
